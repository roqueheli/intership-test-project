import ReactDOM from 'react-dom/client';
import App from './App';

// use ReactDOM's createRoot method to create a root entry point for React rendering.
// render the App component into the root element with the id 'root'.
ReactDOM.createRoot(document.getElementById('root')).render(<App />);
